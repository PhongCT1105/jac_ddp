[project]
name = "jacgrid-jachammer-coordinator"
version = "0.1.0"
description = "JacGrid public coordinator for JacHammer sandbox deployment"
entry-point = "main.jac"
kind = "service"

[serve]
port = 8000

[dependencies]

[dev-dependencies]
watchdog = ">=3.0.0"

[jacpack]
name = 'jac_ddp'
description = 'Jac IDE snapshot for jac_ddp'
