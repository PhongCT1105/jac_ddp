# fixture-token-hash-v1

This foundation model lowercases and tokenizes text, maps every token into a stable 16-dimensional signed bucket, and L2-normalizes the result.

It is deterministic and dependency-free. It is not presented as a production-quality semantic embedding model. Replacing it requires a new immutable workload version.
